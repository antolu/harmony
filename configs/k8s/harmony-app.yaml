apiVersion: apps/v1
kind: Deployment
metadata:
  name: harmony-app
  namespace: harmony
  labels:
    app: harmony-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: harmony-app
  template:
    metadata:
      labels:
        app: harmony-app
    spec:
      containers:
        - name: harmony-app
          image: harmony-app:latest
          ports:
            - containerPort: 80
              name: http
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: harmony-app
  namespace: harmony
  labels:
    app: harmony-app
spec:
  type: ClusterIP
  selector:
    app: harmony-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
